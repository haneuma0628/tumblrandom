<div class="post photo">
    <div class="post-icon">
        <%= fa_icon 'camera' %>
    </div>
    <div class="post-content">
        <% @photos = post['photos']
        @photos.each do |photo| %>
        <%= image_tag photo['original_size']['url'] %>
    <% end %>
    </div>
    <div class="detailed-information">
        <%== post['caption'] %>
    </div>
</div>
